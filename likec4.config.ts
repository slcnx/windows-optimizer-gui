import { defineConfig } from 'likec4/config'

export default defineConfig({
  name: 'windows-optimizer-gui',
  title: 'Windows Optimizer GUI 中文架构图',
  exclude: ['node_modules/**', 'dist/**', 'architecture/png/**'],
})
